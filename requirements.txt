# Python dependencies for ADS-B Feeder Monitor
# Note: Most dependencies come from system packages (GTK4, libadwaita)
# Install system packages with:
#   Ubuntu/Debian: sudo apt install python3-gi gir1.2-gtk-4.0 gir1.2-adw-1
#   Fedora: sudo dnf install python3-gobject gtk4 libadwaita
#   Arch: sudo pacman -S python-gobject gtk4 libadwaita

PyGObject>=3.42.0
